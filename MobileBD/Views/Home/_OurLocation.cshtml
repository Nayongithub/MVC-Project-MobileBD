@model IEnumerable<MobileBD.Models.OurLocation>

@*<div class="card">

    <div class="card-header" style=" background-color: #001131">
        <h3 class="text-center" style="color: #FDA66D">Location</h3>
    </div>
    <div class="list-group">
        @foreach (var l in Model)
        {

            <a class="list-group-item nav-link" href="@Url.Action("ProductsByLocationId","Product",new { id=l.OurLocationID})">
                <span style="color:black">@l.OurLocationName</span>
                <span class="badge pull-right" style="color: #FDA66D">@l.Products.Count</span>
            </a>
        }
    </div>
</div>*@
<div >
    <table style="width:100%">
        <thead style="background-color: #001131">
            <tr class="text-center" style="color: #FDA66D">
                <th class="rounded"><h4>Location</h4></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var l in Model)
            {
            <tr>
                <td>
                    <a class="list-group-item nav-link" href="@Url.Action("ProductsByLocationId","Product",new { id=l.OurLocationID})">
                        <span class="text-muted">@l.OurLocationName</span>
                        <span class="badge pull-right" style="color: #FDA66D">@l.Products.Count</span>
                    </a>
                </td>
            </tr>
            }

        </tbody>
    </table>
</div>
